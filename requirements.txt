# Requires Python >= 3.11. pandas 3.x, numpy 2.4 and scikit-learn 1.9 all declare
# Requires-Python >=3.11, so these pins cannot be installed on an older interpreter.
# Keep this in step with the base image in the Dockerfile: the two drifted apart once
# already, and because the container installs before it runs anything, the mismatch
# surfaced as a failed deploy rather than a test failure.
#
# Pinned to the versions this project is developed and tested against.
# The models are pickled sklearn pipelines, so an unpinned upgrade of pandas,
# scikit-learn or xgboost can silently produce artefacts the API cannot load.
# The Dockerfile retrains inside the image, which keeps the container consistent,
# but a local `pip install` needs these pins to match.

pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.2.0

fastapi==0.137.1
uvicorn==0.49.0
python-multipart==0.0.32
requests==2.34.2

# Computer vision. torch is a hard dependency of ultralytics; it was previously
# pulled in transitively and therefore unpinned.
ultralytics==8.4.72
torch==2.12.1
opencv-python-headless==4.11.0.86

# Used only by backend/data_audit.py to render the EDA charts. Not needed to serve
# the app; install with `pip install -r requirements-dev.txt` if you regenerate them.
